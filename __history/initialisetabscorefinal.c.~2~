#include "initialisetabscorefinal.h"


void initialisetabscorefinal(Joueur* pointeurtab_joueur)
{
   pointeurtab_joueur->tourcourant=0; //on initialise la variable de la structure joueur ''tabscorefinal'' à 0 pour que chaque case soit incrémentable
}
