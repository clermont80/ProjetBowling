#include <stdio.h>
#include <stdlib.h>
#include "inscription.h"


void inscription(Joueur* pointeur_tabjoueur)
{
	scanf("s",pointeur_tabjoueur->speudo);   //le joueur saisit son pseudo
    //créer un tableau de joueur de taille 6 par defaut puis créer un malloc si le nombre de joueur n'est pas 6
}
