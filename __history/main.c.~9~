#pragma hdrstop
#pragma argsused

#include <stdio.h>
#include "joueur.h"

#ifdef _WIN32
#include <tchar.h>
#else
  typedef char _TCHAR;
  #define _tmain main
#endif

int _tmain(int argc, _TCHAR* argv[]) 
{
	int nbjoueurfinal;

	nbjoueurfinal=combiendejoueur();  //on récupère le nombre de joueurs souhaité


		Joueur* tab_joueurmalloc = NULL;
		tab_joueurmalloc = malloc(nbjoueurfinal * sizeof(Joueur)) //on alloue de la mémoire pour le nombre de joueurs souhaités

		if(tab_joueurmalloc == NULL) //on regarde si l'allocation de mémoire a marché
        {
		   exit(0); // On arrête tout car on risque une perte de mémoire
		}


	for(int i=0;i<nbjoueurfinal;i++)
	{
		tourcourant.tab_joueurmalloc[i]=0;
	}

    for(int i=0;i<nbjoueurfinal;i++)
	{
		lancer1(tab_joueurmalloc[i]);
	}




	return 0;
}
